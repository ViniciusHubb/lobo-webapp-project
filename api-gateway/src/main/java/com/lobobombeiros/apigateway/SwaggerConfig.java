package com.lobobombeiros.apigateway;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // A configuração agora é feita de forma automática via application.properties
}
